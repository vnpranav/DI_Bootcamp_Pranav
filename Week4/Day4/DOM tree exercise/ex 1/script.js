// to get div
var container = document.getElementById("container");

var con = document.body.children[0];

// get ul nodes and render each
var lists = document.querySelectorAll("ul > li");
alert(lists);

// to complete
